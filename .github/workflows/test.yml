name: Test Workflow

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: @ParthivS20/kicad-exporter@v1
        with:
          out-files: |
            sch_pdf
            pcb_pdf
            png
            stl
            step